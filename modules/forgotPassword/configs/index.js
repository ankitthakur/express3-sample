var ForgotConfig = function (app, express) {
    
    'use strict';
    
    this.displayName = 'ForgotConfig';
    
    var setup;
    
    setup = function (app, express) {
        
        // for future use
    };
    
    setup(app, express);

};

module.exports = ForgotConfig;